[project]
name = "yaml-sample"
root = "."
languages = ["yaml"]

[[layers]]
name = "config"
paths = ["config/**"]
dependency_mode = "opt-out"
external_mode = "opt-out"
name_deny = ["aws"]

[[layers]]
name = "manifests"
paths = ["manifests/**"]
dependency_mode = "opt-out"
external_mode = "opt-out"
